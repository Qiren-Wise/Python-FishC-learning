测试
0、c的命名不正确，不能以数字开头
1、打印“小甲鱼”
2、打印“小甲鱼”
3、打印“520”
4、有三种方法，可以选择反斜杠（\）来转义；可以用原始字符串（r）；可以用双引号。
5、先单独打完前面的可以直接使用原始数值的字段，后面又两个斜杠表示
6、在例子中小甲鱼起了个 str 名字的变量，但事实上我们发现，str 的颜色跟普通变量貌似不同？
没错，str() 和 print() 都是内置函数，但 Python 貌似不介意我们对内置函数进行赋值操作，所以
这点我们以后就要注意啦，否则可能会出现以下的 BUG。


总结：这节课小甲鱼对变量和长字符串进行了一些简单的说明
变量不能以数字开头，并且应该以一个好记的方式来命名变量；使用变量前要先命名；大小写对于py
来说是不同的。
长字符串是"""内容""",也可以用\n来表示换行，不过比较麻烦。
